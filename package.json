{
  "name": "docmentis-udoc-viewer",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "copy-wasm": "rm -rf packages/udoc-viewer/src/wasm && cp -r ../docmentis-udoc/target/wasm packages/udoc-viewer/src/wasm",
    "dev": "npm run dev --workspace packages/udoc-viewer-demo",
    "build": "npm run build --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit --project packages/udoc-viewer/tsconfig.json",
    "release": "npm run typecheck && npm run lint && npm run format:check && npm run clean --workspace packages/udoc-viewer && npm run build --workspace packages/udoc-viewer && sh -c 'npm version \"$0\" --workspace packages/udoc-viewer --no-git-tag-version && VERSION=$(node -p \"require(\\\"./packages/udoc-viewer/package.json\\\").version\") && git add packages/udoc-viewer/package.json package-lock.json && git commit -m \"v${VERSION}\" && git tag \"v${VERSION}\" && git push && git push --tags'",
    "release-patch": "npm run release -- patch",
    "release-minor": "npm run release -- minor",
    "release-major": "npm run release -- major",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.2",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.3.0",
    "husky": "^9.1.7",
    "prettier": "^3.8.1",
    "typescript-eslint": "^8.56.1"
  }
}
